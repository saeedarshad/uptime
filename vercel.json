{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "prisma generate && DATABASE_URL=\"${DIRECT_URL:-$DATABASE_URL}\" prisma migrate deploy && (DATABASE_URL=\"${DIRECT_URL:-$DATABASE_URL}\" tsx scripts/seed-demos.ts || echo '[build] demo seed skipped') && next build",
  "crons": [
    { "path": "/api/jobs/run", "schedule": "0 8 * * *" }
  ]
}
